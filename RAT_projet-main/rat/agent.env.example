# Agent environment configuration
# Copy this to .env in the rat/ directory before running the agent

# C2 Server address (IP:port of your server)
SERVER_URL=http://192.168.1.10:8080

# Agent Ed25519 identity private key (base64)
# Generate with: cargo run --bin client -- identity
AGENT_IDENTITY_PRIVATE_KEY=

# Agent X25519 pre-key private key (base64, 32 bytes)
# Generate with any random 32-byte base64 string
AGENT_PREKEY_PRIVATE_KEY=

# Operator (client) Ed25519 public key (base64)
# Must match the CLIENT_IDENTITY_PUBLIC_KEY set on the server
CLIENT_IDENTITY_PUBLIC_KEY=
